apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-h.fullname" . }}
  labels:
    {{- include "demo-h.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo-h.selectorLabels" . | nindent 4 }}
